Style: strict TypeScript, ESM with NodeNext, 2-space indentation, .js extensions in local TS imports, camelCase for variables/functions, PascalCase for classes/use cases, kebab-case route filenames. ESLint uses simple-import-sort and eslint-config-prettier. After changes, run pnpm exec tsc --noEmit and pnpm exec eslint ., then manually verify affected API endpoints because no automated test suite is committed yet.